package com.company;

import com.company.package1.*;
import com.company.package2.*;

import java.util.ArrayList;


public class Main {

    public static void main(String[] args) {
        /*Movie[] movies = new Movie[10];
        String[] titles = {"The Godfather","The Shawshank Redemption",
                "The Dark Knight", "12 Angry Men", "Schindler's List", "Pulp Fiction", "Fight Club",
                "Forrest Gump", "Inception", "The Matrix"};
        double highestRating = 0;
        ArrayList<String> bestMovieTitles = new ArrayList<String>();
        for(int i=0;i<10;i++)
        {
            Movie movie = new Movie(titles[i],Math.random()*10);
            System.out.println(movie.getTitle() + ":" + movie.getRating());
            movies[i]=movie;
            if(movie.getRating()>highestRating)
                highestRating = movie.getRating();
        }
        for(int i = 0;i<10;i++)
        {
            if(movies[i].getRating()==highestRating)
                bestMovieTitles.add(movies[i].getTitle());
        }
        for (String title : bestMovieTitles)
            System.out.println(title);
         */
    }
}
